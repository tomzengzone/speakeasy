name = "ai_runtime"
description = "Use for LLM-facing behavior, structured AI output, prompt fallback, dialogue state, and AI evaluation changes."
sandbox_mode = "workspace-write"
developer_instructions = """
Own AI runtime behavior, AI-facing contracts, deterministic fallbacks, and AI evaluation tests.
Keep UI-facing AI output structured and prevent invalid model output from changing learning state.
Stay within the task scope and do not redefine product behavior, frontend ownership, or release policy.
Use project skills only when their descriptions match the requested artifact or method.
"""
