---
schema_version: 1
pr_unit_id: PR-007
title: Flutter 版本固定详情（VS-CONTENT-002-1）与集成验收
status: awaiting_acceptance
revision: 2
approved_revision: 2
approved_at: 2026-08-10T21:31:40+08:00
updated_at: 2026-08-10T22:01:57+08:00
depends_on: PR-006
branch: agent/simplify-governance-and-story-delivery
head: e2881c78985eadfe3371e11d2104f9e599e87576
commit:
remote_pr:
---
# PR-007: Flutter 版本固定详情（VS-CONTENT-002-1）与集成验收

## Objective

独立交付 `VS-CONTENT-002-1` / `FR-CONTENT-002` 的版本固定 Course detail，并完成 001/002
全链集成、观测、灰度前提与可恢复回滚证据，停在用户验收门而不执行生产发布。

## Included Scope

- 从目录 Course summary card 携带同一 `course_id + course_version_id` 打开详情。
- 详情 header 展示同版本英文标题、中文简介、CEFR、典型完成时长及单位；null background 安全占位。
- missing/stale/hidden CourseVersion、失败重试、上下文保留和不替代其他版本/课程的状态。
- 在既有 OpenAPI 语义不变的前提下，补齐可重复生成的 `TypicalDuration`、`CourseDetail`、
  `CourseDetailResponse` typed DTO，以及 `CourseCatalogApi` exact-detail 读取方法和 production adapter；
  `FE-CONTENT` 只消费该 generated/API-client boundary，不手写平行 DTO、HTTP client 或 latest resolver。
- `TC-FR-CONTENT-002`、`TC-VS-CONTENT-002-1`，并回归 PR-006 FR/VS-TC 和整体 Flutter/Spring/API。
- 记录 exact commit 的请求/错误/版本观测证据、灰度启用前提、关闭新入口/读取路径与兼容版本回退证据。

## Excluded Scope

- 开始学习、课程路线/阶段、003–012、生产部署、远程 PR/merge、生产 migration 或正式 release 决策。
- 运行时 latest-version 替换、缓存伪成功、Course 本地事实源、CMS 或新的产品文案承诺。

## Allowed Paths

- `lib/features/content/**`
- `lib/config/app_config.dart`，仅 PR-001 已接受的本地入口灰度/回滚控制
- `scripts/check_openapi_dart_drift.py`，仅确定性生成并校验 exact-detail DTO
- `docs/architecture/openapi/dart-client-drift-manifest.json`，仅在 generator 要求时同步 generated schema 描述
- `lib/generated/api/speakeasy_api.dart`，仅上述 generator 的确定性输出
- `lib/services/api_client.dart`，仅扩展现有 `CourseCatalogApi` exact-detail typed seam
- `test/features/content/course_detail_contract_test.dart`
- `test/features/content/content_catalog_contract_test.dart`，仅必要回归增强
- `test/services/api_client_contract_test.dart`，仅 exact-detail generated DTO、path 与 failure mapping 回归
- `integration_test/course_detail_header_test.dart`
- `docs/task-plans/content-catalog-course-detail-vs-delivery/**`，仅计划状态、checkpoint、观测/灰度/回滚证据

## Acceptance Criteria

- 目录课程卡片传递并解析同一 Course ID/version；详情不得自行读取 latest 或显示其他课程/过期版本。
- 必备字段全部可见且一致；duration > 0 并带单位，background null 不阻断也不显示破损图片错误。
- missing/stale/hidden/failed 状态提供契约规定的恢复或返回路径，并保留可安全展示的已知上下文。
- 四条 canonical Content FR/VS 测试、全量 Flutter、相关 Spring 和 API contract 验证通过。
- Evidence 记录 exact commit SHA、观测检查结果、灰度启用条件、rollback trigger/steps/result；未执行生产发布。

## Verification Commands

- `flutter test test/features/content/course_detail_contract_test.dart`
- `flutter test integration_test/course_detail_header_test.dart`
- `flutter test test/features/content/content_catalog_contract_test.dart`
- `flutter analyze`
- `flutter test`
- `mvn -f backend/pom.xml test`
- `npm run check:api-contract`
- `python scripts/validate_story_slice_delivery.py`
- `git diff --check`

## Governance and Review Requirements

- Accountable implementation owner：`frontend`；集成测试证据 owner：`qa`；灰度/回滚可执行性由 `devops`
  审阅，但本任务不触发 `G-RELEASE`，因为不请求生产发布。
- 适用 `G-TEST`；实现完成后执行独立 code quality review。任何 contract/产品语义偏差返回 owning authority。
- 验证通过后必须转 `awaiting_acceptance` 并停止；用户接受 PR-007 后仍需单独确认总任务验收。

## Current State

Revision 2 已获用户批准并完成本地实现。Flutter 现在从 Course summary 携带同一
`course_id + course_version_id` 请求 typed exact-detail endpoint；详情严格校验 Course、version、标题、
简介、CEFR 与三项 binding identity，拒绝任何跨版本或跨课程替代。实现已通过目标 Flutter、全量
Flutter、相关 Spring Course/Scenario、API contract、drift、Story/Slice、文档语言和静态检查；独立代码
质量审查为 APPROVE，DevOps 本地实现门为 PASS。未提交、推送、部署或执行生产发布。

## Changes Made

- 扩展确定性 OpenAPI Dart generator，生成 `TypicalDuration`、`CourseDetail`、`CourseDetailResponse`，并在
  既有 `CourseCatalogApi` / production adapter 中增加 exact-detail 读取；未新增平行 DTO、HTTP client 或
  latest resolver。
- 新增 Course detail 页面及 catalog navigation，覆盖 loading、known-context retry、401、privacy-safe
  404、invalid response、null background、返回列表焦点和 retry 后 header/error 焦点。
- 对 Course、version、标题、简介、CEFR 和全部 binding identity 做严格一致性校验；逐字段负例证明不显示
  其他 Course/version。
- 增加 `ENABLE_CONTENT_COURSE_DETAIL` 本地构建开关；关闭时课程卡片不可进入详情且不发送 detail 请求。
- 增加 best-effort 脱敏观测：`feature_area`、`result_class`、`request_id`、`ref_hash`、
  `validation_result`；观测失败不影响详情呈现，不记录 authored body 或原始 Course/version UUID。
- 新增/增强 FR、VS、API-client 和 production `AppRoot -> Home -> catalog -> exact detail` widget 测试；
  legacy `LessonDetailPage` 保持不变。

## Evidence

- Repository base HEAD：`e2881c78985eadfe3371e11d2104f9e599e87576`。它是 PR-006 基线，不包含
  PR-007；本任务按计划未 commit/push，不能冒充 PR-007 exact commit。
- Implementation composite：`d49f81efefbde90fd78578660b6d7f4c9f0b82abb4bb1382c7c8c577490215cf`。
  它覆盖 11 个非 task-plan implementation/test 文件，算法为按路径排序后对每行
  `path<TAB>git-hash-object`（LF 结尾）的 UTF-8 payload 计算 SHA-256；task-plan 记录因包含该值而排除。
- `flutter analyze`：通过，无 issue。
- PR-007 目标 Flutter：detail 14/14 通过；catalog production wiring 6/6 通过；API-client contract
  9/9 通过。最终全量 `flutter test`：295/295 通过。
- `mvn -f backend/pom.xml -Dtest=<Course/Scenario related classes> test`：19/19 通过，含 exact detail、
  binding、visibility、cache、rollback、persistence 与相邻 Scenario controller。
- 全量 `mvn -f backend/pom.xml test`：执行 308 项，3 项失败；复跑仍失败。两项因外部 AI provider
  20 秒超时返回 `recoverable_error`，一项为 Goal Autopilot nanosecond/microsecond 时间文本差异；本 PR
  无 backend 文件变更，相关 Course/Scenario 19 项均通过，作为范围外 baseline/environment gap 记录。
- Windows 上 `npm run check:api-contract` 的 package script 使用 POSIX `UV_CACHE_DIR=...` 语法，原命令
  不可执行；等价 Redocly lint、OpenAPI contract、Dart client drift 均通过（89 paths、95 operations、
  209 schemas），Story/Slice delivery、document language 与 `git diff --check` 通过。
- `flutter test integration_test/course_detail_header_test.dart` 因多设备无法自动选择；Chrome 不支持
  integration test，Windows 宿主又缺 `nuget.exe`。Windows 不是产品目标平台；同一链路由可执行的
  platform-independent production AppRoot widget test 覆盖，integration test 源码同时断言 exact identity、
  返回列表 state 与焦点恢复。
- 独立 code quality 最终结论：APPROVE，无 P0/P1/P2；DevOps 最终结论：本地实现门 PASS，生产 rollout
  BLOCKED。未执行生产 migration、远程 PR、部署或 release。
- Rollback dry-run/result：未触发生产回滚；前端 off 测试与既有 backend compatibility rollback contract
  通过。触发条件为 identity/schema mismatch、latest/cross-version substitute、不可见版本泄露或超过发布前
  批准阈值的持续错误。步骤为停止 cohort，以
  `--dart-define=ENABLE_CONTENT_COURSE_DETAIL=false` 关闭 App 详情入口，设置
  `SPEAKEASY_CONTENT_COURSE_READ_ENABLED=false` 后重启/重新部署 backend，验证 `/v1/scenarios` 及
  Scenario/Practice/Training/Learning 保持可用而 Course list/detail 为 404；保留 Course 三张表，不执行
  down migration 或 DML rollback。

## Blockers

- 本地 PR 实现无阻断；等待用户验收。
- 生产 rollout 明确阻断，直至取得包含 candidate composite 的 immutable commit、App artifact digest、
  backend image digest；由获批产品/内容来源提供 published/visible CourseVersion 与有效 binding；完成生产
  migration/config/staged cohort；把本地观测接入 Sentry/backend monitoring 并批准数值阈值；补齐 transport
  failure 的客户端 request-ID 传播。测试 fixture 禁止进入生产。
- 严格命令证据缺口保留为验收可见事实：当前没有 PR-007 exact commit；canonical integration test 无适用
  本机产品设备；全量 Maven 有 3 个范围外失败。不得将 implementation composite 解释为 release artifact。

## Next Action

转入 `awaiting_acceptance` 后停止实现，等待用户验收 PR-007 revision 2；不得自动完成总任务、commit、
push、部署或放开生产 rollout。

## Recent Checkpoints

- 2026-08-07：锁定详情必须版本固定，并将观测、灰度/回滚证据放在最终验收 PR。
- 2026-08-10：用户批准 revision 1；执行前影响分析发现 typed detail DTO/API seam 未落地且不在 Allowed
  Paths。未写应用代码；按计划契约升为 revision 2，补齐最小 generated/API-client/test 路径并等待重批。
- 2026-08-10：用户批准并执行 revision 2；完成 exact-detail DTO/API/UI、identity 防替换、错误恢复、
  可访问性、入口回退与脱敏观测。目标/全量 Flutter 与相关 Spring/API gates 通过；独立 quality APPROVE、
  DevOps 本地 PASS，生产 rollout 保持 BLOCKED，候选等待用户验收。
