---
schema_version: 1
pr_unit_id: PR-002
title: 架构影响与复用边界
status: proposed
revision: 1
approved_revision:
approved_at:
updated_at: 2026-08-11T07:35:41+08:00
depends_on: PR-001
branch:
head:
commit:
remote_pr:
---
# PR-002: 架构影响与复用边界

## Objective

以 PR-001 已批准 Story/FR 为 authority，对现有 003/004 Architecture Coverage Matrix 做局部 impact check，
把 draft compatibility 切换为 approved authority，并明确现有 SWC、数据流、复用与禁止重复边界；逐项证明不触发全局评审且无需新增 ADR。

## Included Scope

- 更新 System Overview、Module Boundary、Software Component Architecture、SWC Catalog 与 Data Flow。
- 固化 Learning route、Course-to-Practice launch、AI opener、authenticated audio playback 四条跨层流。
- 明确 Content authored facts、Learning projection、Practice session/message、AI gateway、Media storage、generated client、AudioService 的所有权。
- 记录六项触发器结论：Course identity、progress ownership、跨 SWC 原子事务、binding identity、provider/storage/security、第三 runtime 均未改变。

## Excluded Scope

- 新 ADR、领域字段、API payload、Prompt schema、UX 或代码实现。
- 新 route/session/message store、新 provider/adapter 或跨 SWC transaction coordinator。

## Allowed Paths

- `docs/architecture/system_overview.md`
- `docs/architecture/module_boundary.md`
- `docs/architecture/software_component_architecture.md`
- `docs/architecture/swc_catalog.md`
- `docs/architecture/data_flow.md`
- `docs/task-plans/content-learning-route-task-challenge-delivery/**`，仅计划状态、checkpoint 与证据

## Context Budget

- 上限：18k tokens。
- 只读取已批准 003/004 Story/FR、现有矩阵两行、ADR-0008 的 Course/version 决策和受影响 SWC/Flow；不重做 Content 001–012 全景分析。

## Acceptance Criteria

- 003/004 的 current authority 与 PR-001 一致；不再标成 draft，也不误改 005–012 状态。
- 四条流逐一写清 producer/consumer、稳定引用、failure boundary、禁止复制组件和 rollback seam。
- 六项全局触发器有逐项 evidence，结论为 `fits-baseline`；文件中明确“无 ADR”，且未创建 ADR 文件。
- `FE-CONTENT` 不拥有 Practice runtime；`BE-CONTENT-SCENARIO` 不拥有 learner progress；音频读取仍在现有 Media security boundary。

## Verification Commands

- `python scripts/check_swc_allocation.py`
- `python scripts/check_cross_cutting_boundaries.py`
- `python scripts/check_document_language.py --scope changed --include-worktree`
- `git diff --check`
- `python .agents/skills/manage-task-plan/scripts/task_plan.py validate content-learning-route-task-challenge-delivery`
- 不运行 Maven、Flyway 或 Flutter 测试。

## Governance and Review Requirements

- Accountable owner：`system_architect`；只对批准事实做局部影响检查。
- 完成后由 `software_architecture_governance_check` 独立只读审查 SWC allocation、复用边界、数据流和 ADR 判定。
- 若任一全局触发器命中，停止本卡并修订计划，不得以“局部文档更新”绕过 ADR/全局评审。

## Current State

架构矩阵已预分类 003/004 为 `fits-baseline`，但 authority 仍是 draft compatibility only。

## Changes Made

None；尚未获本卡批准。

## Evidence

None。

## Blockers

- 依赖 PR-001 完成并获用户验收；本卡 revision 1 还需单独批准。

## Next Action

PR-001 验收后申请本卡批准并执行局部架构 impact check。

## Recent Checkpoints

- 2026-08-11：确认现有 003/004 矩阵为 `fits-baseline`，把复核限制为五份基线文档且不新增 ADR。
